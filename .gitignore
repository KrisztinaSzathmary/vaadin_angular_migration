.settings
.classpath
.project

.idea/
*.iml

**/target/

# Frontend files generated by the plugin
**/node_modules
bookstore-starter-flow-ui/webpack.generated.js
/bin/

# Auto-generated by the `flow:prepare-frontend` Maven goal on every build.
# package.json, package-lock.json, tsconfig.json, types.d.ts and vite.config.ts
# are also plugin-managed but Vaadin's own docs/header comments say to commit
# them (they hold customizable config / dependency locks); only these three
# are pure regenerate-on-build output:
bookstore-starter-flow-ui/frontend/generated/
bookstore-starter-flow-ui/vite.generated.ts
bookstore-starter-flow-ui/.npmrc

# Local-only Claude Code permissions, not shared config
.claude/settings.local.json

